[package]
name = "btstack"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Safe wrappers for btstack-sys focused on port/libusb/main.c workflow"

[dependencies]
btstack-sys = { path = "../btstack-sys" }
